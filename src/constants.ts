// table names
export enum Tables {
	Contracts = "tcontracts"
}

// navigator names
export enum Navigators {
	Contracts = "ContractsNavigator"
}

// routes
export enum Routes {
	ContractsHome = "ContractsHome",
	ContractsEditForm = "ContractsEditForm"
}